# Current Skills Inventory (Windows host)

Date: 2026-08-23 (initial) · **2026-08-24 (addendum: pack metadata, sampled verification, dedup clusters, raidan-os CLI contract, curated-set proposal)**
Scope: global `C:\Users\Raidan\.config\opencode\skills`. Project-level `.opencode/skills`: none on this machine. No `~/.claude`, no `~/.agents`.
Full raw list preserved at docs/research/global-skills-list.txt (committed). Per-skill classification will be machine-generated by `raidan skill doctor` later; this doc records cluster-level findings.
Entry count: 1,566 items = **1,565 skill directories** + `_version.json` (earlier count of 1,564 dirs was off by one).

## Pack Manager Metadata (verified 2026-08-24)

```json
{ "schemaVersion": 2, "version": "10.4.1", "mode": "project", "installedAt": "2026-06-24T00:23:49Z" }
```
Skills use standard `SKILL.md` + YAML frontmatter (`name`, `description`, optional `license`, `compatibility`).
Sampled quality is high — e.g. `context-engineering` already implements MVI/budget discipline that validates our Context Engine design.

## Cluster classification (prefix-based counts)

| Cluster | Count | Status | Notes |
|---|---|---|---|
| azure-* | ~140 | USEFUL (domain) | massive Azure coverage; keep, gate behind profile |
| design-md-* | 60 | USEFUL | brand/design-system references |
| google-cloud/gke/gcloud-* | ~70 | USEFUL | GCP coverage |
| agent-* / agentic-* | ~32 | ACTIVE core | directly relevant to this project |
| opencode-* | 26 | ACTIVE core | includes configuring-opencode, customize-opencode |
| data-/database-* | ~26 | USEFUL | |
| senior-/engineering-/code-review etc. | ~40 | USEFUL | discipline packs |
| product/marketing/content/social/growth | ~60 | USEFUL (non-core) | business clusters |
| security/penetration/threat | ~11+ | USEFUL | review before enabling autonomous use |
| nim-* | 10 | ACTIVE | NIM model configs — feeds provider catalog |
| copilotkit/react/motion/frontend | ~30 | USEFUL (non-core) | frontend stack |
| pinecone/weaviate/qdrant/milvus | ~20 | USEFUL | vector DB ops knowledge |
| delete-*/add-*/update-* (LiteLLM manager set) | ~21 | DUPLICATE RISK | overlapping CRUD skills for LiteLLM proxy — consolidate candidates |
| yemenjpt-* / galyarder-* / presshouse/radio swarms | ~30 | USER-DOMAIN | personal swarm systems — PRESERVE untouched (spec: preserve existing config/user intent) |
| confluent-skill-reviewer evals mock-skills | few | TEST FIXTURES | not real capabilities — exclude from registry scoring |
| template-skill/test-skill/Bad_Frontmatter | few | PLACEHOLDERS | mark UNKNOWN/exclude |

## Key statuses summary
- **ACTIVE** (load-bearing today): opencode-*, agent/agentic engineering core, nim-*, context-*
- **USEFUL**: domain clusters above — retained, exposed via profiles not defaults
- **DUPLICATE candidates**: LiteLLM CRUD family (add/delete/update/list × key/org/team/model/mcp/user/agent ≈ 21 near-identical skills); multiple seo-audit variants; react-expert vs react-patterns vs react-performance overlap
- **UNSAFE-flagged for review**: penetration-testing/offensive suites (require explicit opt-in profile + never in autonomous mode)
- **PRESERVE**: all user-domain swarms (YemenJPT, PressHouse, TaizRadio, Galyarder) — migration engine must never touch these without explicit consent

---

# Addendum — 2026-08-24

## Fine-grained dedup clusters (content-informed)

Beyond the prefix clusters above, name-level analysis across all 1,565 dirs shows these
consolidation candidates for the future **curated set only** (user install stays untouched):

| Cluster | Members | Rec |
|---|---|---|
| Code review | `code-review`, `code-reviewer`, `code-review-expert`, `review`, `reviewer`, `pr-review`, `pr-review-expert`, `receiving-code-review`, `requesting-code-review`, `cs-karpathy-reviewer` | MERGE → one primary |
| Context family | `context-engineering/-engine/-manager/-budget/-compression/-ranking/-retrieval/-optimization/-injection` (9) | MERGE → `context-engineering` (richest, verified) |
| Orchestration guidance | `agentic-os`, `agentic-engineering`, `multi-agent-coordinator`, `team-agent-orchestration`, `workflow-orchestrator`, `plan-orchestrate`, `orch-*`×6 | SPECIALIZE → superseded by kernel + `raidan-os` |
| MCP building | `mcp-builder`, `mcp-developer`, `mcp-server-builder`, `building-mcp-servers`, `mcp-server-patterns` | MERGE → `building-mcp-servers` |
| TDD | `tdd`, `tdd-guide`, `tdd-workflow`, `test-driven-development` | MERGE → `tdd-workflow` |
| Advisor personas | `cs-*`(25) vs `galyarder-*`(7) vs `c-level-*`(3) | Keep ONE family (`cs-*`) in curated set; user copies preserved |

## Raidan-overlap register (skills vs `src/core`)

| Skill(s) | Collides with | Disposition |
|---|---|---|
| **`raidan-os`** ⭐ | entire control plane | **REUSE — canonical bridge skill** (MIT, already targets our CLI). See contract below |
| `agentic-os`, `enterprise-agent-ops` | orchestration | REPLACEABLE-later (kernel supersedes guidance-only skills) |
| `context-*` family | `src/core/context` | KEEP as agent-facing guidance; engine owns enforcement |
| `unified-memory`, `knowledge-*` | `src/core/memory` | KEEP; engine owns storage, skills own technique |
| `add/update/delete-mcp`, `omni-mcp` | `src/core/mcp` | REPLACEABLE-later by `raidan mcp` commands |
| `a2a-engineering`, `omni-agents-a2a` | `integrations/a2a` | KEEP until A2A layer ships |
| `ai-gateway-engineering`, `llm-cost-optimizer`, `litellm-manager` | `src/core/gateway` | KEEP; gateway owns routing, skills own practice |
| `eval-harness`, `agent-eval`, `self-eval` | `core/evaluation` | KEEP; evaluator consumes their methodology |
| `security-audit*`, `threat-modeling` | policies trust levels | KEEP — feeds Review Engine reviewer pool |

## ⚠️ `raidan-os` implicit CLI contract (must-implement list)

The existing skill instructs agents to call commands our CLI must honor:

```
raidan doctor                                  ✅ exists (smoke script)
raidan status                                  required
raidan config show                             required (secrets auto-redacted)
raidan agent list                              required
raidan skill duplicates                        required
raidan task create "<t>" --complexity L0..L4   required
raidan policy check <domain> <action>          required
        domains: filesystem shell network git deployment secrets mcp a2a model-usage cost
raidan model route --need X --prefer cost      required
raidan migrate inspect|plan|apply|rollback     required
```

Verdict semantics assumed: **ALLOW / ASK / DENY** (DENY final; ASK = present to human and wait).
➡️ Acceptance criteria for Phase 3 CLI. Single source of truth = CLI help; the skill mirrors it and must be updated in lockstep on any change.

## Proposed curated shipped set (~24, fills manifest `curated_shipped: []`)

Control plane: `raidan-os` · Context: `context-engineering`, `context-budget` ·
Orchestration: `subagent-driven-development`, `dispatching-parallel-agents`, `verification-loop` ·
Review/Security: code-review (merged), `security-review`, `threat-modeling`, `skill-security-auditor` ·
Testing: `tdd-workflow`, `playwright-expert`, `coverage` ·
Gateway: `ai-gateway-engineering`, `llm-cost-optimizer`, `openai-compatible-api` ·
Protocols: `building-mcp-servers`, `a2a-engineering` ·
Memory: `unified-memory`, `knowledge-graph-creation` ·
Research: `deep-research`, `fact-checking` · Docs: `technical-writer`, `changelog-generator` ·
Identity: `YemenJPT`

Each bundled skill gets `manifest.json` (name/version/capabilities/triggers/license/source) during Phase 3.

## Rules going forward (spec §50–§53)
1. Never duplicate an installed high-quality skill under a new name.
2. New RaidanOpencode skills land in repo `skills/` with ownership manifest entries; installer merges idempotently.
3. Registry scores only curated RaidanOpencode-shipped skills; the 1,565 remain user-owned inventory, inventoried but not adopted wholesale.

## Notable gaps vs spec taxonomy
No dedicated: evaluation-engineering, trajectory-eval, golden-dataset management, A2A-specific operational skills (a2a-engineering exists ✓), runtime-supervisor ops skill. → these ship WITH RaidanOpencode rather than being found.
